# Historical MDX comparison snapshots

These files preserve the previous accordion-based weapon documentation for
comparison, not authoring. They are outside the content collection and produce
no duplicate weapon routes. Edit `src/content/docs/weapons/*-weapons.md` only.

The Hydra snapshot is rendered at `/weapon-layout-comparison/` beside the current
Markdown version. Other snapshots are retained to avoid losing any previous work.
